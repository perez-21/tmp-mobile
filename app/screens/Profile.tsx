import { useColorScheme } from "@/hooks/useColorScheme";
import React from "react";

const Profile = () => {
  const theme = useColorScheme() ?? "light";
  return (
    <></>
    // Add your Profile component UI here, using Colors[theme].PROPERTY for colors
  );
};

export default Profile;
